import React from 'react';

function Homepage() {
  return (
    <section className="homepage">
      <h1>Welcome to Troubled Life</h1>
      <p> Troubled Life clothing, apperal for those who made it through the struggle.</p>
      <a href="troubled-life\src\components" className="cta-button">Shop Now</a>
    </section>
  );
}

export default Homepage;

/* In this component structure, the `Homepage` component represents the homepage section of your website. It includes a headline, a description, and a call-to-action button.

You can customize the content of the `h1`, `p`, and `a` tags to display your desired heading, description, and call-to-action text. Additionally, you can add CSS classes and styles to the component to achieve the desired visual appearance.

Remember to import and use the `Homepage` component in your `App.js` file.

Overview:
The `Homepage` component is a functional component that represents the homepage section of the Troubled Life clothing web application. It displays a headline, description, and a call-to-action button. */